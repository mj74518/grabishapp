import { ShowLocation.Model } from './show-location.model';

describe('ShowLocation.Model', () => {
  it('should create an instance', () => {
    expect(new ShowLocation.Model()).toBeTruthy();
  });
});
