import { ItemLocation.Model } from './item-location.model';

describe('ItemLocation.Model', () => {
  it('should create an instance', () => {
    expect(new ItemLocation.Model()).toBeTruthy();
  });
});
