import { FeedbackModel.Model } from './feedback-model.model';

describe('FeedbackModel.Model', () => {
  it('should create an instance', () => {
    expect(new FeedbackModel.Model()).toBeTruthy();
  });
});
